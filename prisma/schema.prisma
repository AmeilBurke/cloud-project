generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlserver"
  url      = env("DATABASE_URL")
}

model Number {
  NumberId    Int @id(map: "PK__Number__0BED0EBBC0990683")
  numberCount Int @default(0, map: "DF__Number__numberCo__23BE4960")
}
